# Award Auxiliary Service — Service Bus worker
# No FastAPI or gunicorn — plain synchronous worker loop.
# Includes ML libraries for async fraud detection (nomination_submitted handler).

# Environment Variables
python-dotenv>=1.0.0,<2.0.0

# Service Bus
azure-servicebus>=7.12.0,<8.0.0

# Managed Identity auth for Service Bus (DefaultAzureCredential)
azure-identity>=1.15.0,<2.0.0

# Database — SQL Server via ODBC Driver 18
pyodbc>=5.0.0,<6.0.0

# JWT — approve/reject token generation (same as backend token_utils.py)
pyjwt[crypto]>=2.8.0,<3.0.0
cryptography>=41.0.0,<43.0.0

# Observability — Application Insights
azure-monitor-opentelemetry>=1.6.0,<2.0.0 

aiohttp>=3.9.0,<4.0.0

# HTTP client — used by payout_submit.py to call Workday Proxy and Award API
httpx>=0.25.0,<0.28.0

# ML — fraud detection for nomination_submitted handler
# CPU-only torch keeps the image ~1.5 GB lighter than the CUDA variant.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
sentence-transformers>=2.6.0
scikit-learn==1.8.0
numpy>=1.24.0

# Blob storage — stream fraud model pkl from Azure Blob Storage
azure-storage-blob>=12.19.0,<13.0.0
